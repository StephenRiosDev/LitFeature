import { defineConfig } from 'vite'

export default defineConfig({
  // For user/org pages (yourusername.github.io), use '/':
  // base: '/',
  
  // For project pages (yourusername.github.io/repo-name), use: '/repo-name/'
  base: '/LitFeature/',
})
